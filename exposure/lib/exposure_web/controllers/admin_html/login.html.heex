<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin</title>

  <!-- Preload critical fonts -->
  <link rel="preload" href="/fonts/inter/inter-400.ttf" as="font" type="font/ttf" crossorigin />
  <link rel="preload" href="/fonts/dm-mono/dm-mono-400.ttf" as="font" type="font/ttf" crossorigin />
  <link rel="stylesheet" href="/assets/css/app.css" />
</head>
<body class="bg-[#F0F0F0] min-h-screen flex flex-col items-center justify-center p-6 text-[#111] selection:bg-black selection:text-white">

  <div class="w-full max-w-[320px]">
    <div class="bg-white p-8 border border-gray-200 shadow-sm">
      <%= if @error do %>
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded text-sm mb-6">
          <%= @error %>
        </div>
      <% end %>

      <%= if @oidc_enabled do %>
        <a href={~p"/admin/auth/oidc"}
           class="w-full flex items-center justify-center gap-2 bg-neutral-900 py-3 text-white text-[10px] uppercase tracking-[0.2em] hover:bg-neutral-700 transition-colors">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z" />
          </svg>
          Sign in with <%= @oidc_provider_name %>
        </a>

        <%= if @local_auth_enabled do %>
          <div class="relative my-6">
            <div class="absolute inset-0 flex items-center">
              <div class="w-full border-t border-gray-200"></div>
            </div>
            <div class="relative flex justify-center text-[9px] uppercase tracking-wider">
              <span class="bg-white px-3 text-gray-400">or</span>
            </div>
          </div>
        <% end %>
      <% end %>

      <%= if @local_auth_enabled do %>
        <form class="space-y-6" action={~p"/admin/login"} method="post">
          <input type="hidden" name="_csrf_token" value={get_csrf_token()} />

          <div class="space-y-1">
            <input name="username"
                   class="block w-full border-b border-gray-200 bg-transparent py-2 text-sm font-sans placeholder-gray-400 focus:border-black focus:outline-none transition-colors rounded-none"
                   placeholder="Username"
                   autocomplete="username"
                   value={@username || ""}>
          </div>

          <div class="space-y-1">
            <input name="password"
                   class="block w-full border-b border-gray-200 bg-transparent py-2 text-sm font-sans placeholder-gray-400 focus:border-black focus:outline-none transition-colors rounded-none"
                   placeholder="Password"
                   type="password"
                   autocomplete="current-password">
          </div>

          <div class="space-y-1">
            <input type="text" name="totp_code" id="totpCode"
                   class="block w-full border-b border-gray-200 bg-transparent py-2 text-sm font-sans placeholder-gray-400 focus:border-black focus:outline-none transition-colors rounded-none font-mono text-center tracking-widest"
                   placeholder="2FA Code (if enabled)"
                   maxlength="6"
                   pattern="[0-9]*"
                   autocomplete="one-time-code">
            <p class="text-[9px] text-gray-400 mt-1">Enter 6-digit code from your authenticator app</p>
          </div>

          <div class="flex items-center pt-2">
            <input name="remember_me"
                   type="checkbox"
                   class="h-3 w-3 border-gray-300 rounded focus:ring-black focus:ring-offset-0">
            <label class="ml-2 text-[9px] text-gray-600 uppercase tracking-wider">
              Remember Me
            </label>
          </div>

          <button type="submit"
                  class="w-full bg-black mt-4 py-3 text-white text-[10px] uppercase tracking-[0.2em] hover:bg-neutral-800 transition-colors">
            Sign In
          </button>
        </form>
      <% end %>
    </div>

    <div class="mt-6 text-center text-[9px] text-gray-400 uppercase tracking-wider">
      <p>Exposure Admin Panel</p>
    </div>

  </div>

  <%= if @local_auth_enabled do %>
    <script>
      // Auto-format TOTP code input
      document.getElementById('totpCode').addEventListener('input', function(e) {
        e.target.value = e.target.value.replace(/\D/g, '');
      });
    </script>
  <% end %>

</body>
</html>
